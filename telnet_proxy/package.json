{
  "name": "mudslinger-telnet-proxy",
  "version": "0.3.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node dist/telnet_proxy/src/app.js",
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node dist/telnet_proxy/src/connectionTarget.test.js && node test/integration.test.js",
    "postinstall": "node tools/postinstall.js"
  },
  "dependencies": {
    "@types/node": "^24",
    "fs-extra": "^11",
    "socket.io": "^4",
    "typescript": "^5"
  },
  "devDependencies": {
    "socket.io-client": "^4"
  }
}
